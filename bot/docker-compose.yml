# Leverage Copilot Discord relay bot.
# n8n runs with --network host, so the bot must too (reaches localhost:5678).
services:
  leverage-copilot-bot:
    build: .
    container_name: leverage-copilot-bot
    restart: unless-stopped
    network_mode: host
    env_file: .env
